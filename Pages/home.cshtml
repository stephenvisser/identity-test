@page
@model Svper.HomeModel
<h2>Welcome @User.Identity.Name</h2>
<ul>
    @foreach (var claim in User.Claims)
    {
        <li>
            @claim.Type: @claim.Value
        </li>
    }
</ul>
